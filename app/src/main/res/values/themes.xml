<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash Screen Api -->
    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ss_logo_black</item>
        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="postSplashScreenTheme">@style/Theme.VisionAdmin</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.VisionAdmin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme</item>
        <item name="colorPrimaryVariant">@color/theme</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/theme</item>
        <item name="colorSecondaryVariant">@color/theme</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Categories Chip -->
    <style name="Theme.CategoriesChip" parent="Widget.Material3.Chip.Filter.Elevated">
        <item name="chipBackgroundColor">@color/categories_chip_background</item>
        <item name="android:textColor">@color/categories_chip_text</item>
        <item name="checkedIconTint">@color/categories_chip_text</item>
    </style>

    <!-- Prime Chip -->
    <style name="Theme.PrimeChip" parent="Widget.Material3.Chip.Filter.Elevated">
        <item name="chipBackgroundColor">@color/prime_chip_background</item>
        <item name="android:textColor">@color/prime_chip_text</item>
        <item name="checkedIconTint">@color/prime_chip_text</item>
    </style>
</resources>